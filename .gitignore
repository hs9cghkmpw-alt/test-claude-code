__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/

# Pipeline runtime state (fetch intervals, saved patterns, usage tracking).
# Never contains article bodies, only URLs/timestamps/abstracted patterns,
# but it's still local run state that doesn't belong in version control.
/data/
